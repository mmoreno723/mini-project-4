searchEngine();

function searchEngine () {
    var searchBtn = document.getElementById("search-button");
    var searchInput = document.getElementById("search-input");

    searchBtn.addEventListener("click", function() {
        // submit search query based on user input
        
        // if user doesn't select a format, make it a generic search

        // else record the format the user selected 

        // perform a query based on the format

        // if query response is successful, go to search results page
    });
}